<Window x:Class="Aspectly.Example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        Title="Aspectly Bridge Example"
        Width="1200" Height="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <!-- Browser -->
        <cef:ChromiumWebBrowser x:Name="Browser" Grid.Column="0" />

        <!-- Control Panel -->
        <Border Grid.Column="1" Background="#f5f5f5" Padding="16">
            <StackPanel>
                <TextBlock Text="Aspectly Bridge Demo" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />

                <!-- Status -->
                <Border Background="White" CornerRadius="4" Padding="12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Status" FontWeight="SemiBold" />
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Fill="Red" Margin="0,0,8,0" />
                            <TextBlock x:Name="StatusText" Text="Initializing..." />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Actions -->
                <Border Background="White" CornerRadius="4" Padding="12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Call JS Methods" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <Button x:Name="BtnGreet" Content="Call greet()" Click="BtnGreet_Click" Margin="0,0,0,8" Padding="8" />
                        <Button x:Name="BtnGetTime" Content="Call getTime()" Click="BtnGetTime_Click" Margin="0,0,0,8" Padding="8" />
                        <Button x:Name="BtnCalculate" Content="Call calculate(5, 3)" Click="BtnCalculate_Click" Padding="8" />
                    </StackPanel>
                </Border>

                <!-- Log -->
                <Border Background="White" CornerRadius="4" Padding="12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Log" FontWeight="SemiBold" />
                            <Button x:Name="BtnClearLog" Content="Clear" Click="BtnClearLog_Click" Margin="16,0,0,0" Padding="8,2" FontSize="11" />
                        </StackPanel>
                        <ScrollViewer Height="300" VerticalScrollBarVisibility="Auto">
                            <TextBox x:Name="LogBox"
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     BorderThickness="0"
                                     FontFamily="Consolas"
                                     FontSize="11"
                                     Background="Transparent" />
                        </ScrollViewer>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>
